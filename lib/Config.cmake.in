@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/${CMAKE_FIND_PACKAGE_NAME}Targets.cmake")

include(CMakeFindDependencyMacro)

# OpenMP_FOUND=@OpenMP_FOUND@
# if(OpenMP_FOUND)
if(@OpenMP_FOUND@)
    find_dependency(OpenMP)

    # This is not sufficient to have CMake add missing library files and flags when linking client apps
    # add_dependencies(${CMAKE_FIND_PACKAGE_NAME}::${CMAKE_FIND_PACKAGE_NAME} OpenMP::OpenMP_C)

    set_target_properties(${CMAKE_FIND_PACKAGE_NAME}::${CMAKE_FIND_PACKAGE_NAME}
        PROPERTIES
            INTERFACE_LINK_OPTIONS ${OpenMP_C_FLAGS})
endif()

check_required_components(${CMAKE_FIND_PACKAGE_NAME})
